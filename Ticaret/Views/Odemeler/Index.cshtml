@model IEnumerable<Ticaret.Models.Odemeler>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.odeme_tipi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Siparisler1.durum)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.odeme_tipi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Siparisler1.durum)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.odeme_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.odeme_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.odeme_id })
        </td>
    </tr>
}

</table>
